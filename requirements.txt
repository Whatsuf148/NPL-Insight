pandas>=2.0
numpy>=1.24
PyYAML>=6.0
streamlit>=1.30
plotly>=5.18
pytest>=7.4
# Note: win-probability model uses src/logistic_regression.py (pure numpy),
# not scikit-learn — scikit-learn/scipy require compiled extensions that
# may be blocked by OS-level security policies (e.g. Windows Application
# Control). Add scikit-learn back here only if your environment allows it
# and you want to swap in a richer model.
